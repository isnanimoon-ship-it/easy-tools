# 아이디어 백로그

아직 승인되지 않은 제안을 보관하는 공간이다. 이 문서의 항목은 구현 허가가 아니며, Product Owner가 평가해 `CANDIDATE`로 승격해야 한다.

## 작성 템플릿

```md
### 아이디어 이름
- 사용자 문제:
- 예상 사용자/빈도:
- 핵심 입력과 결과:
- 브라우저 단독 처리 가능성:
- 독립 URL/검색 의도:
- 개인정보·보안 고려:
- 예상 복잡도/의존성:
- 제안자/날짜:
- 상태: IDEA
```

## 후보 평가 규칙

사용자 빈도, 문제 명확성, 브라우저 처리 가능성, 단순성, 검색 가치, 개인정보 이점을 각각 1~5점으로 평가한다. 중복 기능, 서버 의존, 유지보수 비용과 범위 확장 위험을 함께 기록한다.

## 보류 아이디어

### Base64 Converter 후속 UX 옵션

- 사용자 문제: 반복 변환, URL 삽입, padding 정책에 맞출 때 수동 편집이 필요함
- 예상 사용자/빈도: 개발자·테스터 / 간헐적
- 핵심 입력과 결과: 입력·출력 Swap, Base64URL, padding 유지·제거
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 Base64 Converter 안의 옵션으로 검토
- 개인정보·보안 고려: 로컬 처리 유지, URL-safe와 암호화를 혼동하지 않는 설명 필요
- 예상 복잡도/의존성: 중간 / 새 의존성 불필요
- 제안자/날짜: Product Owner / 2026-08-26
- 상태: IDEA (`Should Have`, 현재 SPEC 제외)

### Base64 Converter 예제·자동 변환

- 사용자 문제: 첫 사용자가 입력 형식을 모르거나 버튼 반복 실행이 번거로울 수 있음
- 예상 사용자/빈도: 초보 사용자 / 낮음
- 핵심 입력과 결과: locale별 예제 입력, 입력 중 자동 변환
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 Base64 Converter 보조 기능
- 개인정보·보안 고려: 자동 변환 시 legacy chunk 로드와 오류 노출 시점을 명확히 해야 함
- 예상 복잡도/의존성: 낮음~중간 / 새 의존성 불필요
- 제안자/날짜: Product Owner / 2026-08-26
- 상태: IDEA (`Could Have`, 현재 SPEC 제외)

## 전체 도구 고도화 리뷰 (2026-08-30)

19개 도구 전체를 다시 훑으며 각 SPEC의 "Could Have"/"제외 범위"에 이미 적혀 있던 후보와, 이번 리뷰에서 새로 떠오른 아이디어를 함께 정리했다. `Do Not Build`로 명시적으로 거절된 항목(자동 탐지 재도입, YouTube Data API 의존 기능 등)은 다시 올리지 않았다 — 예외적으로 여전히 수요가 큰 항목은 "PO 재검토 필요"로 표시해 신중하게만 남겼다.

### 텍스트

#### 1. Word Counter — 읽기 예상 시간 & 결과 복사
- 사용자 문제: 글자 수만으로는 "이 글이 몇 분짜리인가"를 가늠하기 어렵고, 계산된 통계를 다른 곳에 옮기려면 손으로 옮겨 적어야 함
- 예상 사용자/빈도: 블로그·문서 작성자 / 높음
- 핵심 입력과 결과: 분당 평균 읽기 속도 상수로 예상 시간 계산, 통계 4개를 한 번에 텍스트로 복사
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 없음(순수 계산)
- 예상 복잡도/의존성: 낮음 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — "계산 결과" 영역에 예상 읽기 시간(한/일은 공백 제외 글자 수 기준, 그 외 언어는 단어 수 기준 분당 속도로 계산)과 통계 4개 + 읽기 시간을 한 번에 복사하는 버튼을 추가. 텍스트가 비어 있으면 둘 다 숨김. 유닛 테스트 8개 추가, 기존 QA 전체 재검증 PASS

#### 2. Text Cleaner — Regex 기반 정리 규칙
- 사용자 문제: 공백/중복 정리로 해결 안 되는 패턴(예: 특정 접두사 줄만 제거)은 지금 처리할 수 없음
- 예상 사용자/빈도: 개발자·데이터 정리 사용자 / 중간
- 핵심 입력과 결과: 사용자 정의 정규식 찾기·바꾸기 규칙을 파이프라인에 추가 단계로 삽입
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 Could Have(SPEC에 이미 후보로 기록됨)
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 중간(잘못된 정규식 오류 처리 필요) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — 정규식 치환을 다른 옵션보다 먼저 적용되는 독립 단계로 구현. 프리셋 전환 시 규칙이 사라지지 않도록 처리, 잘못된 패턴은 크래시 대신 인라인 오류로 표시. 유닛 테스트 10개, 기존 QA 전체 재검증 PASS

#### 3. Korean Initial Consonant Converter — 결과 파일 다운로드
- 사용자 문제: 긴 텍스트를 초성으로 변환한 뒤 파일로 보관하고 싶을 때 복사 후 직접 저장해야 함
- 예상 사용자/빈도: 초성 퀴즈·콘텐츠 제작자 / 낮음~중간
- 핵심 입력과 결과: 현재 결과를 `.txt`로 다운로드
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 낮음 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DECLINED — Product Owner 판단: 진행하지 않기로 결정(2026-08-30).

### 개발자

#### 4. JSON Formatter — 안전한 구문 강조 + 트리 뷰
- 사용자 문제: 큰 JSON을 평문으로만 보면 구조 파악이 어렵고, 특정 키를 찾기 위해 눈으로 스캔해야 함
- 예상 사용자/빈도: 개발자 / 높음
- 핵심 입력과 결과: key/string/number/boolean/null 색상 구분, 객체·배열 접기/펼치기
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: `jwt-decoder`에서 이미 검증한 "정규식 토크나이저 + React 텍스트 노드만 사용, `dangerouslySetInnerHTML` 금지" 패턴을 그대로 재사용하면 XSS 위험 없이 구현 가능 — 새로 설계할 필요 없음
- 예상 복잡도/의존성: 중간(트리 접기 상태 관리) / 의존성 없음, 하이라이터 코드 재사용 가능
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — "편집/트리 보기" 모드 토글 추가. 트리 보기는 jwt-decoder의 정규식 토크나이저 패턴을 재사용해 key/string/number/boolean/null을 색상으로 구분하고, 객체·배열을 노드별로 접기/펼치기 가능(전체 펼치기·전체 접기 버튼 포함). 유효하지 않은 JSON일 때는 안내 문구로 대체. 유닛 테스트 9개(트리 빌더) + 7개(컴포넌트) 추가, 기존 QA 전체 재검증 PASS

#### 4. JSON Formatter — 키 검색 (이어서)
- 사용자 문제: 수백 줄짜리 JSON에서 특정 key를 눈으로 찾기 어려움
- 예상 사용자/빈도: 개발자 / 중간
- 핵심 입력과 결과: 검색어 입력 시 일치하는 key/value 경로 하이라이트 및 이동
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 중간 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — 트리 보기 모드에 키 검색창 추가. 대소문자 무시 부분 일치로 키를 찾아 일치 개수(N/총)를 표시하고, 이전·다음 버튼으로 순환 이동하며 이동 시 접혀 있던 상위 노드를 자동으로 펼치고 스크롤. 일치하는 키가 없으면 안내 문구 표시. 유닛 테스트로 이동/자동 펼침/대소문자 무시 검증 완료

#### 5. Base64 Converter — 파일 → Base64 / Data URI 미리보기
- 사용자 문제: 작은 이미지·폰트 파일을 Data URI로 인라인해야 할 때 별도 변환기를 찾아야 함
- 예상 사용자/빈도: 프론트엔드 개발자 / 중간
- 핵심 입력과 결과: 파일 선택 → Base64 문자열 + `data:` URI + (이미지면) 미리보기
- 브라우저 단독 처리 가능성: 가능(FileReader만으로 충분)
- 독립 URL/검색 의도: 기존 페이지 내 옵션 또는 별도 탭
- 개인정보·보안 고려: 파일은 브라우저에서만 처리, 서버 전송 없음 — `privacy-redactor`/`favicon-generator`에서 이미 검증된 업로드 패턴 재사용 가능
- 예상 복잡도/의존성: 낮음~중간 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: CANDIDATE — Product Owner 재승인 완료(2026-08-30). 원 SPEC의 제외 결정을 뒤집고 진행. 착수 순서상 후순위로 보류(2026-08-30) — #6 이후 재개 예정

#### 6. URL Encoder/Decoder — Query String 테이블 편집기
- 사용자 문제: 여러 query parameter가 있는 URL을 한 줄 문자열로만 다루면 개별 값 수정이 번거로움
- 예상 사용자/빈도: 개발자 / 중간
- 핵심 입력과 결과: URL 입력 시 key/value 표로 분해해 개별 인코딩·삭제·추가, 다시 조합한 URL 실시간 반영
- 브라우저 단독 처리 가능성: 가능(`URLSearchParams` 활용)
- 독립 URL/검색 의도: 기존 페이지 내 별도 탭
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 중간 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — "텍스트 변환/Query String 편집기" 보기 전환 추가. URL 또는 Query String을 붙여넣고 "분해"하면 key/value 표로 편집(추가/삭제/수정) 가능하며 재구성된 URL이 실시간 반영됨. `URLSearchParams` 기반이라 `+`↔공백 처리가 자연스럽게 해결됨(SPEC에서 별도 검토로 미뤘던 항목). 유닛 테스트 17개(파싱/재구성) + 9개(컴포넌트) 추가, 기존 QA 전체 재검증 PASS

#### 7. QR Code Generator — 구조화 입력 도우미(Wi-Fi·연락처·이메일)
- 사용자 문제: Wi-Fi 공유용 QR, 명함용 vCard QR을 만들려면 정확한 포맷 문자열을 직접 알아야 함
- 예상 사용자/빈도: 일반 사용자·소상공인 / 중간~높음
- 핵심 입력과 결과: 탭으로 Wi-Fi(SSID/비밀번호/보안유형), vCard(이름/전화/이메일), 이메일, 전화, SMS, 위치 입력 폼 제공 → 표준 포맷 문자열 자동 생성 후 QR 인코딩
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 소스 탭 확장(텍스트/URL과 동일한 자리)
- 개인정보·보안 고려: 입력값(Wi-Fi 비밀번호 등)이 서버로 전송되지 않음을 명확히 안내
- 예상 복잡도/의존성: 중간(포맷별 검증) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — "소스 유형" 선택(텍스트/URL·Wi-Fi·연락처·이메일·전화·SMS·위치) 추가. 각 탭은 전용 입력 폼을 제공하고 표준 포맷 문자열(WIFI:, vCard, mailto:, tel:, SMSTO:, geo:)을 자동 생성해 QR로 인코딩. 위치는 위경도 범위를 검증해 잘못된 값이면 QR을 만들지 않음. 유닛 테스트 22개 추가, QA에서 각 포맷을 jsQR로 실제 디코딩해 왕복 검증 완료

#### 7. QR Code Generator — 로고 삽입 (이어서, PO 재검토 필요)
- 사용자 문제: 브랜드 로고가 중앙에 들어간 QR을 요구하는 사용자가 많음
- 예상 사용자/빈도: 마케팅·소상공인 / 중간
- 핵심 입력과 결과: 로고 이미지 업로드 → QR 중앙에 합성(에러 정정 레벨 자동 상향)
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 로고 이미지 로컬 처리만
- 예상 복잡도/의존성: 중간(스캔 가능성 보장을 위한 에러 정정 레벨 계산 필요) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — 중앙 로고 업로드·합성 기능 추가. 로고가 있으면 오류 복원 수준을 자동으로 Q 이상까지 상향(사용자가 이미 Q/H를 선택했다면 유지)하고, 미리보기에 "실제 적용 수준" 표시. 로고 제거 시 원래 수준으로 되돌아감. QA에서 실제로 jsQR로 로고 합성된 QR을 디코딩해 스캔 가능성을 직접 검증(성공)

#### 8. Regex Tester — 여러 테스트 케이스 표
- 사용자 문제: 하나의 패턴을 여러 입력 문자열에 대해 한 번에 검증하고 싶을 때 매번 텍스트를 바꿔 넣어야 함
- 예상 사용자/빈도: 개발자 / 중간
- 핵심 입력과 결과: 테스트 문자열을 행으로 여러 개 추가, 각 행에 매치 여부·캡처 그룹을 표로 표시
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 낮음~중간 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — "여러 테스트 케이스" 섹션 추가. 행을 추가해 여러 문자열을 등록하면 같은 Pattern·Flags로 각 행의 매치 여부·매치 개수·첫 매치의 캡처 그룹(번호·이름 있는 그룹)을 표시. 기존 단일 테스트와 동일하게 Worker 기반 실행이라 catastrophic backtracking에도 안전(행별 1초 타임아웃). 유닛 테스트 6개 추가, 브라우저 QA에 ReDoS 타임아웃 내성 확인을 포함한 배치 시나리오 추가

#### 9. Cron Expression Generator — crontab 전체 줄 생성
- 사용자 문제: 표현식만 만들고 나면 실제 crontab에 넣을 명령어 줄 형식(따옴표·`%` 이스케이프)은 직접 챙겨야 함
- 예상 사용자/빈도: 서버 운영자 / 낮음~중간
- 핵심 입력과 결과: 명령어 입력란 추가 → `cron표현식 command` 형태로 조합, `%` 자동 이스케이프
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 Should Have 확장
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 낮음(이스케이프 규칙만 정확하면 됨) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — 결과 영역에 "명령어 (선택)" 입력란을 추가. 입력하면 `cron표현식 command` 형태의 crontab 전체 줄을 별도 코드 블록으로 보여주고 전용 Copy 버튼을 제공하며, crontab이 특별하게 해석하는 `%` 문자를 자동으로 `\%`로 이스케이프한다. 명령어는 실행되지 않고 브라우저에서 문자열 조합만 수행하며, 초기화 시 함께 지워진다. 유닛 테스트 5개(이스케이프·조합 규칙) 추가, 기존 QA 스크립트에 명령어 입력·조합 결과 검증·복사·초기화 시나리오 추가 후 전체 재검증 PASS

#### 10. JWT Decoder — Claim 값 개별 복사 + JSON 접기
- 사용자 문제: `exp` 하나만 복사하고 싶어도 지금은 Payload 전체를 복사해야 함, 큰 Payload는 스크롤이 길어짐
- 예상 사용자/빈도: 개발자 / 중간
- 핵심 입력과 결과: 각 claim 행에 작은 복사 아이콘, 중첩 객체/배열 접기·펼치기 토글
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 복사 시에도 서버 전송 없음(기존 원칙 유지)
- 예상 복잡도/의존성: 낮음~중간 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — Header/Payload를 평문 코드 블록 대신 접기·펼치기 가능한 트리 뷰로 교체(`components/tools/jwt-decoder/json-tree-view.tsx`, JSON Formatter의 트리 패턴을 이 도구 전용으로 재구현해 재사용, 도구 간 직접 import는 하지 않는 기존 프로젝트 관례 유지). 각 key/value 행과 객체·배열 컨테이너에 작은 복사 아이콘을 붙여 문자열은 따옴표 없이, 객체·배열은 해당 서브트리만 pretty JSON으로 복사한다. 값 복사도 클립보드 API만 사용하며 서버 전송 없음(기존 원칙 유지). 유닛 테스트 9개(`json-tree.ts`) 추가, 기존 QA 스크립트에 claim 개별 복사·접기/펼치기 시나리오 추가 후 전체 재검증 PASS

### 미디어

#### 11. YouTube Thumbnail Downloader — 프레임 썸네일(0/1/2/3.jpg) 추가
- 사용자 문제: 기본 5종 외에 영상 중간 프레임 썸네일이 필요한 경우가 있음
- 예상 사용자/빈도: 콘텐츠 제작자 / 낮음
- 핵심 입력과 결과: 기존 결과 목록에 `0.jpg`~`3.jpg` 4종 추가(가용 여부는 기존 방식대로 실제 로드 성공 여부로 판별)
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 확장
- 개인정보·보안 고려: 기존과 동일(이미지 요청만 발생, 저장 없음)
- 예상 복잡도/의존성: 낮음 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DECLINED(2026-08-30) — 1차 구현 후 되돌림. 구현 전 실제 CDN 응답을 직접 확인(curl + JPEG 헤더 파싱)한 결과 `0.jpg`는 hqdefault와 완전히 동일한 480×360 중복 이미지였고, `1.jpg`~`3.jpg`는 실제 성공 시에도 120×90 고정이라 다운로드해서 쓸 수 있는 품질이 아님을 사용자가 실제 화면 스크린샷으로 확인. 유튜브 CDN 자체가 이 세 엔드포인트에 더 큰 해상도를 제공하지 않아 우리 쪽에서 개선할 여지가 없는 구조적 한계로 판단해 4종 추가를 전부 되돌리고 기존 5종(max/sd/hq/mq/default)만 유지하기로 결정. Product Owner 판단: 진행하지 않기로 결정(2026-08-30).

#### 12. Image Color Picker — 주요 색상 팔레트 추출 + 다운로드
- 사용자 문제: 픽셀 하나씩 클릭하는 대신, 이미지 전체의 대표 색상 조합(팔레트)을 한눈에 보고 싶음
- 예상 사용자/빈도: 디자이너 / 중간~높음
- 핵심 입력과 결과: 이미지 업로드 시 대표 색상 5~8개 자동 추출, HEX 목록 또는 PNG/JSON으로 다운로드
- 브라우저 단독 처리 가능성: 가능(양자화 알고리즘은 순수 JS)
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 기존과 동일(브라우저 전용 처리)
- 예상 복잡도/의존성: 중간(색상 양자화 성능, 큰 이미지는 Worker 검토) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: PASS — Product Owner 판단으로 보류, 진행하지 않음(2026-08-30)

#### 13. Image Compressor — 다중 이미지 일괄 압축 + ZIP 다운로드
- 사용자 문제: 여러 장을 압축할 때 한 장씩 반복해야 함
- 예상 사용자/빈도: 블로거·쇼핑몰 운영자 / 높음
- 핵심 입력과 결과: 여러 파일 선택 → 동일 설정으로 일괄 압축 → 하나의 ZIP으로 다운로드
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 페이지 내 확장
- 개인정보·보안 고려: 기존과 동일(브라우저 전용 처리)
- 예상 복잡도/의존성: 중간 / **`favicon-generator`에 이미 도입한 `fflate`를 재사용하면 새 의존성 없이 구현 가능** — SPEC 작성 당시 예상보다 구현 난이도가 낮아졌음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — 기존 SPEC의 PO 결정("단일 이미지 안정화와 독립된 큐·메모리 범위 필요")대로 단일/여러 이미지 탭을 분리하고, 여러 이미지 탭은 별도의 큐 상태(파일별 대기/처리 중/완료/실패)로 완전히 독립시켜 구현했다(기존 단일 이미지 흐름의 state·effect는 전혀 건드리지 않음). 압축 설정(품질/목표 용량/출력 형식/최대 가로)은 두 탭이 공유하며, "일괄 압축 시작" 클릭 시 순차 처리 후 fflate(`level:0`, 이미 압축된 이미지를 재압축하지 않음)로 ZIP을 묶어 다운로드한다. 동일 파일명 충돌은 " (2)", " (3)" 접미사로 자동 회피, 잘못된 형식의 파일은 개별 행에서만 실패 처리되고 나머지는 계속 진행하며 ZIP에서 자동 제외된다. 한 번에 최대 50장 제한. 유닛 테스트 6개(ZIP 패킹·파일명 중복 회피) 추가, 실제 브라우저 QA에 부분 실패·ZIP 내용 검증(unzip해서 정확히 성공한 파일만 포함되는지 확인)·초과 개수 거부·초기화·모바일 320px 시나리오 추가해 기존 단일 이미지 QA와 함께 전체 재검증 PASS

#### 14. Screenshot Stitcher ↔ Screenshot Status Bar Remover 연동
- 사용자 문제: 여러 스크린샷을 이어붙이기 전에 각 장의 상태바를 일일이 다른 도구에서 지워야 함
- 예상 사용자/빈도: 두 도구를 함께 쓰는 사용자 / 중간
- 핵심 입력과 결과: Screenshot Stitcher에 이미지 추가 시 "상태바 자동 제거 후 이어붙이기" 옵션 제공(내부적으로 `screenshot-statusbar-remover`의 감지 로직 재사용)
- 브라우저 단독 처리 가능성: 가능
- 독립 URL/검색 의도: 기존 두 페이지 모두에서 상호 링크로 발견 가능
- 개인정보·보안 고려: 기존과 동일
- 예상 복잡도/의존성: 중간~높음(두 도구의 상태 모델을 통합해야 함) / 의존성 없음, 기존 감지 알고리즘 재사용
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: PASS — Product Owner 판단으로 보류, 진행하지 않음(2026-08-30)

#### 15. Privacy Redactor — 가림 영역 프리셋 저장
- 사용자 문제: 자동 탐지를 없애고 수동 전용으로 전환한 뒤, 반복되는 형태(예: 전화번호 줄 크기)를 매번 새로 그려야 함
- 예상 사용자/빈도: 반복 작업 사용자(고객센터 스크린샷 등) / 중간
- 핵심 입력과 결과: 자주 쓰는 영역 크기·비율을 이름 붙여 저장, 클릭 한 번으로 같은 크기 영역을 캔버스에 추가(위치는 매번 드래그로 조정)
- 브라우저 단독 처리 가능성: 가능(설정은 `localStorage`에만 저장, 이미지 자체는 저장하지 않음 — 기존 개인정보 원칙과 상충하지 않음)
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 저장되는 것은 "크기 비율" 숫자뿐, 이미지·좌표·내용은 저장하지 않음을 명확히 구분
- 예상 복잡도/의존성: 낮음~중간 / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: PASS — Product Owner 판단으로 보류, 진행하지 않음(2026-08-30)

#### 16. Favicon Generator — Preset 스타일 갤러리
- 사용자 문제: 색상·도형을 처음부터 조합하기 번거로운 사용자는 "이런 느낌" 몇 가지 중 골라 시작하고 싶어함
- 예상 사용자/빈도: 비개발자·빠른 시작 원하는 사용자 / 중간
- 핵심 입력과 결과: 미리 정의된 배경/도형/폰트 조합 6~8종을 썸네일로 제공, 클릭 시 현재 텍스트·이모지 값에 그대로 적용
- 브라우저 단독 처리 가능성: 가능(기존 `renderFavicon` 재사용)
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 없음
- 예상 복잡도/의존성: 낮음(고정 데이터 + 기존 렌더 함수 재사용) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — 배경·도형·테두리·굵기 조합 8종(오션/선셋/포레스트/그레이프/차콜/아웃라인/로즈/앰버)을 탭 목록 바로 아래에 썸네일 갤러리로 제공. 클릭하면 항상 "도형" 탭으로 전환하면서, 클릭 시점에 사용자가 텍스트/이모지 탭에 입력해 둔 값을 그대로 도형의 내부 표시 내용으로 이어받아 적용한다(이미 도형 탭에 있었다면 현재 내부 표시 설정은 그대로 유지). 모든 프리셋 색상 조합은 기존 저대비 경고 기준(3:1)을 통과하도록 사전 계산해, 프리셋 적용 직후 경고가 뜨는 모순을 방지했다. 유닛 테스트 4개(저대비 통과·id 중복 없음·radius 범위·최소 6종) 추가, 브라우저 QA에 텍스트/이모지 탭에서 프리셋 적용 후 탭 전환·색상·도형·테두리·내부 표시 값 유지 여부를 검증하는 시나리오 추가 후 기존 QA 전체와 함께 재검증 PASS

#### 17. Password Generator — Passphrase(단어 조합) 모드
- 사용자 문제: 무작위 문자 비밀번호는 기억하기 어렵고, 최근에는 단어 조합(`correct-horse-battery-staple` 스타일)이 보안성과 기억 용이성 균형으로 널리 권장됨
- 예상 사용자/빈도: 일반 사용자 / 높음
- 핵심 입력과 결과: 단어 개수(3~6개) 선택, 구분자·숫자/대문자 삽입 옵션, 로컬 단어 목록에서 무작위 조합
- 브라우저 단독 처리 가능성: 가능(EFF 스타일 공개 단어 목록을 정적 자산으로 포함)
- 독립 URL/검색 의도: 기존 페이지 내 모드 탭 추가("무작위 문자" / "단어 조합")
- 개인정보·보안 고려: 단어 목록은 정적 자산, 생성 결과 저장 없음(기존 원칙 유지)
- 예상 복잡도/의존성: 중간(단어 목록 자산 크기 관리) / 의존성 없음(자체 단어 목록 필요)
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: DONE(2026-08-30) — "무작위 문자"/"단어 조합" 탭을 추가. 단어 조합은 EFF의 실제 공개 Diceware 단어 목록(eff_large_wordlist.txt, 7,776단어, CC BY 3.0 US, 구현 전 공식 출처에서 직접 다운로드해 7,776개·중복 없음·전부 소문자英 알파벳(하이픈 4개 포함, 원본 그대로)임을 실측 검증)을 정적 자산으로 번들해 매 단어를 서로 독립적으로 무작위 추출한다(표준 Diceware 모델대로 중복 단어 허용). 단어 개수(3~6), 구분자(하이픈/밑줄/마침표/공백/없음), 각 단어 첫 글자 대문자, 숫자 1개 추가 옵션을 제공. 강도 미터는 기존 문자 비밀번호와 동일한 `estimateEntropy(개수, 7776)` 계산을 그대로 재사용하고, 삽입 숫자의 추가 비트는 의도적으로 보수적으로 집계하지 않는다. 유닛 테스트 19개(단어 목록 무결성 포함) + 컴포넌트 테스트 5개 추가, 브라우저 QA에 옵션 조합·범위 오류·복사·오프라인 생성(단어 목록이 정적 번들이라 네트워크 없이 동작)·비밀번호 미노출 검증 시나리오를 추가해 기존 QA와 함께 3로케일 전체 재검증 PASS

#### 18. IP Info — Reverse DNS + 결과 JSON 다운로드
- 사용자 문제: 조회 결과를 팀에 공유하거나 기록으로 남기려면 화면을 캡처하는 수밖에 없음
- 예상 사용자/빈도: 네트워크 관리자·개발자 / 낮음~중간
- 핵심 입력과 결과: 조회 결과에 hostname(역방향 DNS) 필드 추가, "JSON으로 다운로드" 버튼
- 브라우저 단독 처리 가능성: 부분적(hostname 조회는 현재 provider가 지원해야 함, 다운로드 자체는 완전 가능)
- 독립 URL/검색 의도: 기존 페이지 내 옵션
- 개인정보·보안 고려: 기존과 동일(조회 대상 IP만 provider에 전송)
- 예상 복잡도/의존성: 낮음(다운로드) ~ 중간(hostname, provider 의존) / 의존성 없음
- 제안자/날짜: Product Owner / 2026-08-30
- 상태: PASS — Product Owner 판단으로 보류, 진행하지 않음(2026-08-30)
